<div class="brief_topic">
  <input type="checkbox" id="<%= interest.snake_name %>" name="<%= interest.snake_name %>" value="<%= interest.name %>" />
  <label for="<%= interest.snake_name %>" class="tag large"><%= interest.name %></label>
</div>
